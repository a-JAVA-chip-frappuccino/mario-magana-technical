import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'accommodation-testimonial-pictures.s3.us-east-1.amazonaws.com'],
  },
};

export default nextConfig;
